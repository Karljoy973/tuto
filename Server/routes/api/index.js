const user_api = require('@api/user');
const users_api = require('@api/users');
const home_api = require('@api/home');

const api = {
    user_api,
    users_api,
    home_api,
};

module.exports = api;
